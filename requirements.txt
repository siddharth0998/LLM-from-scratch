# Core (used by the package and scripts)
torch>=2.0
tiktoken>=0.5
transformers>=4.40      # pretrained GPT-2 weight loading
pyyaml>=6.0             # config files (configs/*.yaml)

# Notebook / plotting extras (optional)
numpy>=1.24
matplotlib>=3.7
tqdm>=4.66
